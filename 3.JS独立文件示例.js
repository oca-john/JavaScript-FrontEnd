function ShowMess(){                        // 独立JS文件只包含html文件中<script>部分代码
alert("Hello JS, Input your name please."); // 注意不包含<script>标记
}

// Chrome浏览器中调试F12，选Console，可直接以交互模式使用JS代码
// `window.alert('Hello JavaScript!')`, 会直接显示弹出式对话框显示信息

// HTML构建基本界面元素，CSS实现布局优化，JS增加页面动作，则可以在网页上实现所有视觉内容。如果基于React货VUE这样的本地JS框架，则可以用于构建具备交互能力的本地应用程序。
